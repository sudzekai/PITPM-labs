<Window x:Class="LabWork8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        FontSize="18"
        FontFamily="Segoe UI"
        Background="#4c024c">
    <Window.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="gray"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="CaretBrush" Value="#800080"/>
            <Setter Property="SelectionBrush" Value="#800080"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                        CornerRadius="5"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">

                            <ScrollViewer x:Name="PART_ContentHost"
                                  Focusable="False"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden"
                                  Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#800080"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#800080"/>
                                <Setter TargetName="border" Property="Background" Value="#f1f1f1f1"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#800080"/>
                                <Setter TargetName="border" Property="Background" Value="#f1f1f1f1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Purple"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0 10 0 0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                    CornerRadius="5"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">

                            <ContentPresenter
                        Content="{TemplateBinding Content}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"/>

                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#a600a6"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="#a600a6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Label Content="Мега новая страница" FontSize="28" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Border VerticalAlignment="Center" 
                HorizontalAlignment="Center" 
                CornerRadius="15"
                BorderBrush="Purple"
                BorderThickness="1"
                Background="#ffffff"
                Padding="10"
                Visibility="{Binding AuthVisibility}">
            <StackPanel>
                <Label Content="Вход" HorizontalAlignment="Center" FontWeight="Bold" FontSize="25" Padding="0" Margin="0 0 0 10"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="Логин:" VerticalAlignment="Center"/>
                    <TextBox Name="LoginTextBox"
                             Grid.Row="0" 
                             Grid.Column="1" 
                             Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Пароль:" VerticalAlignment="Center" Margin="0 5 0 0"/>
                    <TextBox Name="PasswordTextBox"
                             Grid.Row="1" 
                             Grid.Column="1" 
                             Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 5 0 0"/>
                    
                </Grid>

                <Button Name="AuthButton" 
                        Content="Войти" 
                        Height="30" 
                        Width="150" 
                        Command="{Binding AuthCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
